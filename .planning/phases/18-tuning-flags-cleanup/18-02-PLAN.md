---
phase: 18-tuning-flags-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/doctor.rs
  - README.md
autonomous: true
requirements: [CLN-01, CLN-02]

must_haves:
  truths:
    - "contentops doctor shows VAD (Silero V5) check result"
    - "README doctor section mentions VAD check"
    - "README pipeline flags table has no --breaths row"
  artifacts:
    - path: "src/commands/doctor.rs"
      provides: "VAD health check via VoiceActivityDetector::builder().build()"
    - path: "README.md"
      provides: "Updated doctor output example with VAD check, no --breaths in flags"
  key_links:
    - from: "src/commands/doctor.rs"
      to: "voice_activity_detector crate"
      via: "VoiceActivityDetector::builder()"
      pattern: "VoiceActivityDetector::builder"
---

<objective>
Add VAD health check to the doctor subcommand and update README to reflect current CLI (no --breaths, VAD check in doctor output).

Purpose: Users can verify ONNX Runtime initializes correctly via `contentops doctor`, and docs reflect the current state.
Output: Doctor displays VAD check, README updated.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tuning-flags-cleanup/18-RESEARCH.md
@src/commands/doctor.rs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VAD health check to doctor and update README</name>
  <files>src/commands/doctor.rs, README.md</files>
  <action>
  1. In `src/commands/doctor.rs`:
     - Add `use voice_activity_detector::VoiceActivityDetector;` at the top
     - Add a new check function:
       ```rust
       fn check_vad() -> CheckResult {
           match VoiceActivityDetector::builder()
               .sample_rate(16000i64)
               .chunk_size(512usize)
               .build()
           {
               Ok(_) => CheckResult {
                   name: "VAD (Silero V5)".to_string(),
                   status: Status::Ok,
                   detail: String::new(),
               },
               Err(e) => CheckResult {
                   name: "VAD (Silero V5)".to_string(),
                   status: Status::Fail,
                   detail: format!("({})", e),
               },
           }
       }
       ```
     - Add `check_vad()` to the `checks` vec in `pub fn run()`, after the existing checks (after `check_on_path("claude")`)

  2. In `README.md`:
     - In the pipeline flags table, remove the `--breaths` row entirely (the row containing `| \`--breaths\` | Also detect and remove breaths | off |`)
     - Add `--vad-threshold` and `--min-silence-ms` rows to the pipeline flags table:
       ```
       | `--vad-threshold <THRESHOLD>` | VAD speech probability threshold (0.0-1.0) | `0.5` |
       | `--min-silence-ms <MS>` | Minimum silence duration to cut (ms) | `400` |
       ```
     - Also add the same two rows to the cut command flags table if one exists
     - Verify the doctor section text is accurate (it currently says "Verifies all tools are installed and each subcommand is ready to use" which remains correct)
  </action>
  <verify>
  Run `cargo build 2>&1` -- must compile without errors.
  Run `cargo run -- doctor 2>&1` -- must show "VAD (Silero V5)" line with [ok] or [fail] status.
  Check README.md contains no --breaths references and includes --vad-threshold / --min-silence-ms.
  </verify>
  <done>Doctor displays VAD health check. README reflects current CLI flags with no --breaths and new VAD tuning flags documented.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds
- `contentops doctor` output includes "VAD (Silero V5)" check
- README.md pipeline flags table has --vad-threshold and --min-silence-ms, no --breaths
- No references to --breaths remain in README.md
</verification>

<success_criteria>
- Doctor shows VAD (Silero V5): OK when ONNX Runtime initializes
- Doctor shows VAD (Silero V5): FAILED with error when it cannot initialize
- README accurately documents current CLI surface
</success_criteria>

<output>
After completion, create `.planning/phases/18-tuning-flags-cleanup/18-02-SUMMARY.md`
</output>
