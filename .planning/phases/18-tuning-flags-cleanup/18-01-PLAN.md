---
phase: 18-tuning-flags-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.rs
  - src/vad.rs
  - src/silence.rs
  - src/ffmpeg.rs
  - src/commands/cut.rs
  - src/commands/pipeline.rs
  - tests/silence_tests.rs
autonomous: true
requirements: [VAD-04, VAD-05, CLN-01, CLN-02]

must_haves:
  truths:
    - "contentops cut input.mp4 -o out.mp4 --vad-threshold 0.3 runs without error"
    - "contentops cut input.mp4 -o out.mp4 --min-silence-ms 600 runs without error"
    - "contentops cut --help shows --vad-threshold and --min-silence-ms flags"
    - "contentops cut --help and contentops pipeline --help show no --breaths flag"
    - "silence.rs contains only SpeechInterval, build_concat_filter, adjust_timestamps, total_silence_from_speeches"
    - "ffmpeg.rs contains no run_silencedetect function"
  artifacts:
    - path: "src/cli.rs"
      provides: "CutArgs and PipelineArgs with vad_threshold and min_silence_ms, no breaths"
    - path: "src/vad.rs"
      provides: "run_vad accepting threshold and min_silence_ms parameters"
    - path: "src/silence.rs"
      provides: "Only SpeechInterval, build_concat_filter, adjust_timestamps, total_silence_from_speeches"
  key_links:
    - from: "src/cli.rs"
      to: "src/vad.rs"
      via: "args.vad_threshold and args.min_silence_ms threaded through cut.rs and pipeline.rs"
      pattern: "vad::run_vad.*threshold.*min_silence"
---

<objective>
Add --vad-threshold and --min-silence-ms CLI flags to cut and pipeline commands, remove --breaths flag, and delete all dead amplitude-based silence detection code.

Purpose: Give users control over VAD sensitivity while removing obsolete code paths from the pre-VAD era.
Output: Clean CLI with two new tuning flags, no dead code.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tuning-flags-cleanup/18-RESEARCH.md
@src/cli.rs
@src/vad.rs
@src/silence.rs
@src/ffmpeg.rs
@src/commands/cut.rs
@src/commands/pipeline.rs
@tests/silence_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tuning flags and remove --breaths from CLI args</name>
  <files>src/cli.rs, src/vad.rs, src/commands/cut.rs, src/commands/pipeline.rs</files>
  <action>
  1. In `src/cli.rs`:
     - Remove `breaths` field from `CutArgs` (line 56: `pub breaths: bool`)
     - Remove `breaths` field from `PipelineArgs` (lines 141-142)
     - Add to `CutArgs` after `dry_run`:
       ```rust
       /// VAD speech probability threshold (0.0-1.0, lower = more sensitive)
       #[arg(long, default_value = "0.5", value_parser = clap::value_parser!(f32).range(0.0..=1.0))]
       pub vad_threshold: f32,

       /// Minimum silence duration in milliseconds to cut
       #[arg(long, default_value = "400", value_parser = clap::value_parser!(u32).range(1..))]
       pub min_silence_ms: u32,
       ```
     - Add identical `vad_threshold` and `min_silence_ms` fields to `PipelineArgs`

  2. In `src/vad.rs`:
     - Change `run_vad` signature to accept `threshold: f32` and `min_silence_ms: u32`
     - Remove the `const VAD_THRESHOLD: f32 = 0.5;` constant
     - Use the `threshold` parameter instead of `VAD_THRESHOLD` in the `.label()` call
     - After building the raw `speeches` vector (after the `if in_speech` trailing block and the `last.end` clamp), add a min-silence merge pass:
       ```rust
       // Merge speech segments separated by gaps shorter than min_silence_ms
       if !speeches.is_empty() && min_silence_ms > 0 {
           let min_gap = min_silence_ms as f64 / 1000.0;
           let mut merged = Vec::new();
           let mut current = speeches.remove(0);
           for next in speeches {
               if next.start - current.end < min_gap {
                   current.end = next.end;
               } else {
                   merged.push(current);
                   current = next;
               }
           }
           merged.push(current);
           speeches = merged;
       }
       ```
     - Return the merged speeches

  3. In `src/commands/cut.rs`:
     - Pass `args.vad_threshold` and `args.min_silence_ms` to `vad::run_vad()`:
       `let speeches = vad::run_vad(&wav_path, video_duration, args.vad_threshold, args.min_silence_ms)?;`
     - Remove all commented-out DEPRECATED blocks (lines 14-19, 51-57, 88-92, 133-134)

  4. In `src/commands/pipeline.rs`:
     - Add `vad_threshold: f32` and `min_silence_ms: u32` parameters to `run_stages()` signature, replacing `_breaths: bool`
     - Update the `run_stages()` call in `run()` to pass `args.vad_threshold` and `args.min_silence_ms` instead of `args.breaths`
     - Pass `vad_threshold` and `min_silence_ms` to `vad::run_vad()`:
       `let speeches = vad::run_vad(&wav_path, video_duration, vad_threshold, min_silence_ms)?;`
     - Remove all commented-out DEPRECATED blocks (lines 14-19, 140-150, 187-190)
  </action>
  <verify>
  Run `cargo build 2>&1` -- must compile without errors or warnings about unused variables/imports.
  Run `cargo run -- cut --help` -- must show `--vad-threshold` and `--min-silence-ms`, must NOT show `--breaths`.
  Run `cargo run -- pipeline --help` -- must show `--vad-threshold` and `--min-silence-ms`, must NOT show `--breaths`.
  </verify>
  <done>Both cut and pipeline accept --vad-threshold and --min-silence-ms, neither shows --breaths in --help</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead amplitude code from silence.rs, ffmpeg.rs, and tests</name>
  <files>src/silence.rs, src/ffmpeg.rs, tests/silence_tests.rs</files>
  <action>
  1. In `src/silence.rs`, delete these items entirely:
     - `SilenceInterval` struct (lines 1-5)
     - `parse_silencedetect()` function (lines 13-52)
     - `silence_to_speech()` function (lines 54-112)
     - `words_to_speech_intervals()` function (lines 139-181) -- dead code, not called anywhere
     - `filter_silences_by_words()` function (lines 183-198)
     - `total_silence_removed()` function (lines 234-243)

     The file should contain ONLY:
     - `SpeechInterval` struct
     - `build_concat_filter()` function
     - `adjust_timestamps()` function
     - `total_silence_from_speeches()` function

  2. In `src/ffmpeg.rs`:
     - Delete the entire `run_silencedetect()` function (lines 116-139, including the `// DEPRECATED` comment and `#[allow(dead_code)]`)

  3. In `tests/silence_tests.rs`:
     - Update the import line to only import what remains:
       `use contentops::silence::{SpeechInterval, build_concat_filter};`
     - Delete ALL tests for removed functions:
       - All `parse_silencedetect` tests (parse_empty_input_returns_empty through parse_trailing_with_preceding_pair)
       - All `silence_to_speech` tests (speech_no_silence_returns_full_duration through speech_multiple_silences)
       - All `total_silence_removed` tests (total_removed_single_silence through total_removed_short_silence_under_padding)
     - Keep ALL `build_concat_filter` tests (concat_filter_single_segment, concat_filter_multiple_segments, concat_filter_empty_speeches)

  4. Search the entire src/ directory for any remaining references to:
     - `SilenceInterval`
     - `parse_silencedetect`
     - `silence_to_speech`
     - `filter_silences_by_words`
     - `words_to_speech_intervals`
     - `total_silence_removed`
     - `run_silencedetect`
     - `SILENCE_THRESHOLD_DB`
     - `SILENCE_MIN_DURATION`
     - `BREATH_THRESHOLD_DB`
     - `BREATH_MIN_DURATION`
     - `SPEECH_PADDING`
     Remove any found references (comments or code).
  </action>
  <verify>
  Run `cargo build 2>&1` -- must compile cleanly with no dead_code warnings.
  Run `cargo test 2>&1` -- all remaining tests pass.
  Run `grep -r "SilenceInterval\|parse_silencedetect\|silence_to_speech\|filter_silences_by_words\|total_silence_removed\|run_silencedetect\|SILENCE_THRESHOLD\|BREATH_THRESHOLD\|SPEECH_PADDING\|words_to_speech_intervals" src/ tests/` -- must return no matches.
  </verify>
  <done>silence.rs contains only SpeechInterval, build_concat_filter, adjust_timestamps, total_silence_from_speeches. ffmpeg.rs has no run_silencedetect. No dead amplitude references remain anywhere in src/ or tests/.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with no warnings
- `cargo test` passes all remaining tests
- `cargo run -- cut --help` shows --vad-threshold (default 0.5) and --min-silence-ms (default 400), no --breaths
- `cargo run -- pipeline --help` shows --vad-threshold and --min-silence-ms, no --breaths
- `cargo run -- cut --vad-threshold 1.5 input.mp4` rejects with range error
- `cargo run -- cut --min-silence-ms 0 input.mp4` rejects with range error
- No references to amplitude-based detection remain in source
</verification>

<success_criteria>
- VAD-04: --vad-threshold flag works on both cut and pipeline with validation
- VAD-05: --min-silence-ms flag works on both cut and pipeline with validation
- CLN-01: --breaths flag completely removed
- CLN-02: All dead amplitude code removed from silence.rs, ffmpeg.rs, cut.rs, pipeline.rs, tests
</success_criteria>

<output>
After completion, create `.planning/phases/18-tuning-flags-cleanup/18-01-SUMMARY.md`
</output>
