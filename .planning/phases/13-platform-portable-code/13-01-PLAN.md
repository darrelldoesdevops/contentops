---
phase: 13-platform-portable-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/overlay.rs
  - src/commands/normalize.rs
  - src/error.rs
autonomous: true
requirements:
  - XPLAT-01
  - XPLAT-02
  - XPLAT-03

must_haves:
  truths:
    - "Default font resolves to a valid system font on macOS, Linux, and Windows without --font flag"
    - "normalize command uses cross-platform null output (not /dev/null literal)"
    - "Error hints show brew on macOS, apt on Linux, choco on Windows"
  artifacts:
    - path: "src/commands/overlay.rs"
      provides: "Platform-conditional DEFAULT_FONT + Linux runtime probe"
      contains: "cfg(target_os"
    - path: "src/commands/normalize.rs"
      provides: "Cross-platform null muxer output"
      contains: "\"-\""
    - path: "src/error.rs"
      provides: "Platform-aware install hint functions"
      contains: "ffmpeg_install_hint"
  key_links:
    - from: "src/commands/overlay.rs"
      to: "build_title_filter"
      via: "font_path resolution uses cfg-guarded constants + Linux probe"
      pattern: "resolve_default_font|DEFAULT_FONT"
    - from: "src/error.rs"
      to: "format_error() + #[error] attributes"
      via: "helper functions return platform-specific hint strings"
      pattern: "ffmpeg_install_hint|whisper_install_hint"
---

<objective>
Make contentops compile and run correctly on Linux and Windows by replacing macOS-specific hardcoded paths and hints with platform-conditional alternatives.

Purpose: Remove three macOS-only assumptions blocking cross-platform builds.
Output: Three modified source files with platform-portable code paths.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-platform-portable-code/13-RESEARCH.md
@src/commands/overlay.rs
@src/commands/normalize.rs
@src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform-conditional font path and cross-platform null muxer</name>
  <files>src/commands/overlay.rs, src/commands/normalize.rs</files>
  <action>
In overlay.rs, replace the single `const DEFAULT_FONT` (line 112) with platform-conditional constants:

1. Add three `#[cfg]`-guarded definitions:
   - `#[cfg(target_os = "macos")]` → `"/System/Library/Fonts/Supplemental/Impact.ttf"`
   - `#[cfg(target_os = "windows")]` → `"C:\\Windows\\Fonts\\impact.ttf"`
   - `#[cfg(not(any(target_os = "macos", target_os = "windows")))]` → define `FONT_CANDIDATES: &[&str]` array with paths: msttcorefonts Impact, Liberation Sans Bold, DejaVu Sans Bold (include both `/usr/share/fonts/truetype/` and `/usr/share/fonts/` variants)

2. Add a `resolve_default_font()` function (cfg-gated to non-macOS/non-Windows) that iterates `FONT_CANDIDATES` with `std::path::Path::new(p).exists()` and returns first match, falling back to the first candidate if none found.

3. Update the `font_path` binding in `build_title_filter` (line 130-134) to use:
   - On macOS/Windows: `DEFAULT_FONT.to_string()`
   - On Linux/other: `resolve_default_font()`
   Use cfg-gated blocks inside the `unwrap_or_else` closure.

In normalize.rs, change `"/dev/null"` (line 73) to `"-"`. The `-f null -` combination is ffmpeg's cross-platform null muxer. No cfg needed.
  </action>
  <verify>
Run `cargo build` — must compile on the current platform. Run `cargo check` to confirm no warnings. Grep overlay.rs for `cfg(target_os` to confirm platform branching exists. Grep normalize.rs for `"/dev/null"` to confirm it's gone.
  </verify>
  <done>
overlay.rs has platform-conditional font resolution (macOS const, Windows const, Linux runtime probe with fallback). normalize.rs uses `-` instead of `/dev/null`. `cargo build` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Platform-aware error hints for ffmpeg and whisper</name>
  <files>src/error.rs</files>
  <action>
Add two helper functions at module level in error.rs:

```rust
fn ffmpeg_install_hint() -> &'static str {
    if cfg!(target_os = "macos") {
        "brew install ffmpeg"
    } else if cfg!(target_os = "windows") {
        "choco install ffmpeg"
    } else {
        "apt install ffmpeg"
    }
}

fn whisper_install_hint() -> &'static str {
    if cfg!(target_os = "macos") {
        "brew install whisper-cli"
    } else {
        "build from source: https://github.com/ggerganov/whisper.cpp"
    }
}
```

Note: whisper-cli has no canonical apt/choco package — use "build from source" link for non-macOS per research.

Update four locations to use these helpers:

1. `#[error]` attribute on FfmpegNotFound (line 8): Cannot call functions in `#[error]` derive strings. Instead, make the derive string generic (e.g., `"ffmpeg not found on PATH"`) and leave the platform-specific hint only in `format_error()`.

2. `#[error]` attribute on WhisperNotFound (line 28): Same approach — make generic (e.g., `"whisper-cli not found on PATH"`), platform hint in `format_error()` only.

3. `format_error()` FfmpegNotFound arm (line 83-88): Replace hardcoded `"brew install ffmpeg"` with `ffmpeg_install_hint()`.

4. `format_error()` WhisperNotFound arm (line 123-128): Replace hardcoded `"brew install whisper-cli"` with `whisper_install_hint()`.

Do NOT change ClaudeNotFound — Claude Code CLI is platform-agnostic (same install on all platforms).
  </action>
  <verify>
Run `cargo build` — must compile. Grep error.rs for `"brew install ffmpeg"` — should appear only inside `ffmpeg_install_hint()`, not as a bare literal. Grep for `"brew install whisper-cli"` — should appear only inside `whisper_install_hint()`.
  </verify>
  <done>
error.rs uses platform-aware helper functions for install hints. `format_error()` shows brew on macOS, apt on Linux, choco on Windows for ffmpeg. whisper hint shows brew on macOS, build-from-source link on other platforms. `#[error]` derive strings are platform-neutral. `cargo build` succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds with no errors
2. `cargo clippy` passes with no warnings
3. Grep confirms no remaining `/dev/null` literal in normalize.rs
4. Grep confirms no remaining hardcoded macOS font path as the sole DEFAULT_FONT
5. Grep confirms platform-conditional `cfg` usage in overlay.rs and error.rs
</verification>

<success_criteria>
- `cargo build` compiles successfully
- overlay.rs resolves font paths per-platform (macOS/Windows const, Linux probe)
- normalize.rs uses `-f null -` (cross-platform)
- error.rs shows platform-appropriate install hints
- No hardcoded macOS-only paths or commands remain outside cfg guards
</success_criteria>

<output>
After completion, create `.planning/phases/13-platform-portable-code/13-01-SUMMARY.md`
</output>
