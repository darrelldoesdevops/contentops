---
phase: 14-linux-windows-ci-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - XPLAT-04
  - XPLAT-05

must_haves:
  truths:
    - "Tag push builds Linux and Windows binaries alongside existing macOS binaries"
    - "GitHub Release contains contentops-x86_64-unknown-linux-gnu and contentops-x86_64-pc-windows-msvc.exe with SHA256 checksums"
    - "CI runs fmt, clippy, test, and audit on macOS, Linux, and Windows"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Linux and Windows build jobs + release asset upload"
      contains: "x86_64-unknown-linux-gnu"
    - path: ".github/workflows/ci.yml"
      provides: "Three-platform CI test matrix"
      contains: "matrix"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "build-linux and build-windows jobs"
      via: "release job needs array includes new build jobs"
      pattern: "build-linux|build-windows"
    - from: ".github/workflows/ci.yml"
      to: "matrix strategy"
      via: "runs-on uses matrix.os"
      pattern: "matrix.*os"
---

<objective>
Add Linux and Windows build targets to release.yml and expand CI to test on all three platforms.

Purpose: Enable cross-platform binary distribution so Linux and Windows users can download contentops from GitHub Releases.
Output: Updated release.yml with Linux/Windows build jobs, updated ci.yml with three-platform matrix.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/release.yml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Linux and Windows build jobs to release.yml</name>
  <files>.github/workflows/release.yml</files>
  <action>
Add two new build jobs to release.yml, following the existing per-architecture job pattern (NOT matrix strategy, per context decision):

1. **build-linux** job:
   - `runs-on: ubuntu-latest`
   - Install Rust toolchain with `targets: x86_64-unknown-linux-gnu`
   - Use `Swatinem/rust-cache@v2`
   - `cargo build --release --target x86_64-unknown-linux-gnu`
   - Upload artifact named `contentops-x86_64-unknown-linux-gnu`
   - Path: `target/x86_64-unknown-linux-gnu/release/contentops`

2. **build-windows** job:
   - `runs-on: windows-latest`
   - Install Rust toolchain with `targets: x86_64-pc-windows-msvc`
   - Use `Swatinem/rust-cache@v2`
   - `cargo build --release --target x86_64-pc-windows-msvc`
   - Upload artifact named `contentops-x86_64-pc-windows-msvc`
   - Path: `target/x86_64-pc-windows-msvc/release/contentops.exe`

3. **Update release job:**
   - Add `build-linux` and `build-windows` to `needs:` array: `needs: [build-arm64, build-x86_64, build-linux, build-windows]`
   - Add download steps for Linux and Windows artifacts (same pattern as ARM64/x86_64 downloads)
   - In "Prepare binaries" step: add `chmod +x` for Linux binary (not needed for Windows .exe), rename downloaded binaries to their final names:
     - Linux: `contentops-x86_64-unknown-linux-gnu`
     - Windows: `contentops-x86_64-pc-windows-msvc.exe`
   - In "Generate checksums" step: add shasum lines for Linux and Windows binaries
   - In "Create GitHub Release" files list: add the 4 new files (binary + sha256 for each platform)

Keep existing macOS jobs (build-arm64, build-x86_64, universal binary creation) completely unchanged.
Keep update-tap job completely unchanged.
  </action>
  <verify>
Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`. Grep for `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-msvc` in release.yml. Verify `needs:` array contains all 4 build jobs. Verify files list in release step contains Linux and Windows binaries.
  </verify>
  <done>
release.yml has build-linux and build-windows jobs alongside existing macOS jobs. Release job downloads all 4 platform artifacts, generates checksums, and uploads all binaries to GitHub Release. YAML is valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand CI to three-platform matrix</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Refactor ci.yml from a single `macos-latest` job to a matrix strategy:

1. Replace the `check` job with a matrix-based job:
   ```yaml
   check:
     name: Check (${{ matrix.os }})
     runs-on: ${{ matrix.os }}
     strategy:
       matrix:
         os: [macos-latest, ubuntu-latest, windows-latest]
   ```

2. Keep all existing steps unchanged:
   - actions/checkout@v4
   - dtolnay/rust-toolchain@stable with components: rustfmt, clippy
   - Swatinem/rust-cache@v2
   - cargo fmt --check
   - cargo clippy -- -D warnings
   - cargo test
   - cargo install cargo-audit + cargo audit

3. The `cargo-audit` install step may need `--locked` flag which already exists. No other changes needed -- Rust toolchain and cargo commands work identically across all three platforms.
  </action>
  <verify>
Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`. Grep for `matrix` in ci.yml. Grep for `ubuntu-latest` and `windows-latest` in ci.yml. Verify the strategy section exists.
  </verify>
  <done>
ci.yml uses matrix strategy to run checks on macOS, Ubuntu, and Windows. All three platforms run fmt, clippy, test, and audit. YAML is valid.
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` succeeds
2. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` succeeds
3. release.yml contains `build-linux` and `build-windows` jobs
4. release.yml release job `needs:` includes all 4 build jobs
5. release.yml files list includes Linux and Windows binaries with checksums
6. ci.yml uses matrix strategy with 3 OS targets
7. No changes to existing macOS build jobs or update-tap job
</verification>

<success_criteria>
- release.yml builds Linux (x86_64-unknown-linux-gnu) and Windows (x86_64-pc-windows-msvc) binaries
- GitHub Release would contain all platform binaries with SHA256 checksums
- ci.yml runs fmt, clippy, test, audit on macOS, Linux, and Windows
- Existing macOS release flow unchanged
- All YAML files are syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/14-linux-windows-ci-release/14-01-SUMMARY.md`
</output>
