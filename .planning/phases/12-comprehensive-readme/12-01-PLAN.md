---
phase: 12-comprehensive-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06]

must_haves:
  truths:
    - "README has a prerequisites section listing FFmpeg, whisper-cli, and Claude CLI (optional) with install commands"
    - "README includes both brew install darrelldoesdevops/tap/contentops and direct-download install path"
    - "README leads with a complete contentops pipeline example that is copy-paste ready"
    - "README contains a flag reference table for each subcommand matching --help output exactly"
    - "README troubleshooting section maps error messages from doctor and error hints to resolution steps"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      contains: "contentops pipeline"
---

<objective>
Write README.md from live `--help` output: hero pipeline example, prerequisites, install paths, per-subcommand usage with flag reference tables, and troubleshooting derived from error.rs hints and doctor output.

Purpose: A user landing on the repo can understand, install, and run contentops without consulting any other source.
Output: README.md at repo root.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-readme/12-CONTEXT.md
@src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md from live --help output</name>
  <files>README.md</files>
  <action>
Create `README.md` at the repo root. The content MUST be derived from live `--help` output -- run each subcommand with `--help` and use the exact flags shown.

**Structure (in this order):**

1. **Title and one-liner**: `# contentops` followed by a single sentence describing what it does (video post-production CLI replacing CapCut)

2. **Hero example**: A complete `contentops pipeline` invocation showing input -> output. Copy-paste ready. Show the expected output filename.
   ```bash
   contentops pipeline --model ~/path/to/ggml-base.en.bin input.mp4
   # Runs: cut → caption → overlay
   # Output: input_pipeline.mp4
   ```

3. **Prerequisites**: Table with 3 rows:
   | Prerequisite | Install | Required for |
   |---|---|---|
   | FFmpeg | `brew install ffmpeg` | All commands |
   | whisper-cli | `brew install whisper-cli` | caption, pipeline |
   | Claude CLI | [install link] | overlay --auto (optional) |

4. **Install**: Two paths:
   - Homebrew: `brew install darrelldoesdevops/tap/contentops`
   - Direct download: `curl -L` one-liner from GitHub releases (use latest release URL pattern: `https://github.com/darrelldoesdevops/contentops/releases/latest/download/contentops-aarch64-apple-darwin`)

5. **Usage**: Pipeline first, then each subcommand. For each subcommand:
   - One copy-paste example showing the most common invocation
   - Flag reference table with columns: Flag | Description | Default
   - Derive ALL flags from running `contentops <subcommand> --help` -- no phantom flags, no missing flags

   Subcommands in this order: `pipeline`, `cut`, `caption`, `overlay`, `doctor`

6. **Troubleshooting**: Map these error messages to resolution steps (from src/error.rs):
   | Error | Cause | Fix |
   |---|---|---|
   | `ffmpeg not found on PATH` | FFmpeg not installed | `brew install ffmpeg` |
   | `whisper-cli not found on PATH` | whisper-cli not installed | `brew install whisper-cli` |
   | `whisper model not found: <path>` | Model file doesn't exist | Download from huggingface.co/ggerganov/whisper.cpp |
   | `claude not found on PATH` | Claude CLI not installed | Install Claude Code CLI |
   | `input file not found: <path>` | Wrong path | Check file path |
   | `no speech detected` | Silent video | Video must contain speech for silence removal |

Keep the README under ~200 lines. Tables and code blocks over prose. No marketing language.

**CRITICAL: Run `contentops <subcommand> --help` for EACH subcommand to get exact flags. Do NOT guess or copy from plan text -- the live output is the source of truth.**
  </action>
  <verify>
1. README.md exists at repo root
2. Contains `contentops pipeline` example in first 20 lines
3. Contains `brew install darrelldoesdevops/tap/contentops`
4. Contains flag reference tables for all 5 subcommands
5. Every flag in the tables matches `contentops <subcommand> --help` output
6. Contains troubleshooting section with error -> fix mappings
7. Prerequisites section lists FFmpeg, whisper-cli, Claude CLI
  </verify>
  <done>
- README.md at repo root with hero example, prerequisites, install, usage with flag tables, troubleshooting
- All flags match live --help output exactly
- Both brew and direct-download install paths included
  </done>
</task>

</tasks>

<verification>
1. `head -20 README.md` shows pipeline example near the top
2. `grep -c 'brew install' README.md` shows at least 2 matches (tap + ffmpeg)
3. For each subcommand, diff the flags in README against `contentops <subcommand> --help` output -- no phantom or missing flags
4. `grep 'ffmpeg not found' README.md` returns a match (troubleshooting)
5. `grep 'whisper-cli not found' README.md` returns a match (troubleshooting)
6. `grep 'contentops pipeline' README.md` returns a match (hero example)
</verification>

<success_criteria>
- README leads with pipeline example a user can copy-paste immediately
- Prerequisites section has FFmpeg, whisper-cli, Claude CLI with install commands
- Both Homebrew and direct-download install paths present
- Flag reference table for every subcommand matches --help output exactly
- Troubleshooting maps error messages to resolution steps
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-readme/12-01-SUMMARY.md`
</output>
