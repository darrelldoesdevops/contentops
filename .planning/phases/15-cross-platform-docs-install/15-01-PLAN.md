---
phase: 15-cross-platform-docs-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - XPLAT-06

must_haves:
  truths:
    - "README includes Linux install one-liner with curl and chmod"
    - "README includes Windows install one-liner with PowerShell"
    - "Prerequisites section shows platform-specific install commands for ffmpeg and whisper"
  artifacts:
    - path: "README.md"
      provides: "Cross-platform install and prerequisite documentation"
      contains: "Linux"
  key_links:
    - from: "README.md"
      to: "GitHub Releases"
      via: "Download URLs reference linux-gnu and windows-msvc binaries"
      pattern: "x86_64-unknown-linux-gnu|x86_64-pc-windows-msvc"
---

<objective>
Update README with Linux and Windows install paths and platform-specific prerequisites.

Purpose: Enable Linux and Windows users to install and configure contentops.
Output: Updated README.md with cross-platform documentation.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-platform install and prerequisites to README</name>
  <files>README.md</files>
  <action>
Update README.md with the following changes:

1. **Prerequisites table**: Replace brew-only commands with platform-conditional hints:
   - FFmpeg: `brew install ffmpeg` (macOS) / `apt install ffmpeg` (Linux) / `choco install ffmpeg` (Windows)
   - whisper-cli: `brew install whisper-cli` (macOS) / build from source (Linux/Windows)
   - Format as: show macOS first, then Linux, then Windows in each row

2. **Install section**: Add Linux and Windows install paths after the existing macOS entries:

   **Direct download (Linux x86_64):**
   ```bash
   curl -L -o /usr/local/bin/contentops \
     https://github.com/darrelldoesdevops/contentops/releases/latest/download/contentops-x86_64-unknown-linux-gnu \
     && chmod +x /usr/local/bin/contentops
   ```

   **Direct download (Windows x86_64):**
   ```powershell
   Invoke-WebRequest -Uri https://github.com/darrelldoesdevops/contentops/releases/latest/download/contentops-x86_64-pc-windows-msvc.exe -OutFile contentops.exe
   ```
   Note: Move to a directory on your PATH, or add the current directory to PATH.

3. **Troubleshooting table**: Update ffmpeg and whisper-cli fix column to show platform alternatives:
   - ffmpeg fix: `brew install ffmpeg` (macOS), `apt install ffmpeg` (Linux), `choco install ffmpeg` (Windows)
   - whisper-cli fix: `brew install whisper-cli` (macOS), build from source (Linux/Windows)

Keep all existing content. Do not change any command documentation or flag tables.
  </action>
  <verify>
Grep README.md for "Linux" -- must appear. Grep for "x86_64-unknown-linux-gnu" -- must appear in install section. Grep for "Windows" -- must appear. Grep for "Invoke-WebRequest" -- must appear. Grep for "apt install" -- must appear in prerequisites.
  </verify>
  <done>
README includes Linux curl one-liner, Windows PowerShell one-liner, and platform-specific prerequisite install commands. Existing macOS content unchanged.
  </done>
</task>

</tasks>

<verification>
1. README.md contains Linux install one-liner
2. README.md contains Windows install one-liner
3. Prerequisites table shows apt and choco alternatives
4. Troubleshooting table shows platform-specific fixes
5. Existing content (usage, flags, examples) unchanged
</verification>

<success_criteria>
- Linux users can find install instructions in README
- Windows users can find install instructions in README
- Prerequisites section shows platform-appropriate commands
- Doctor hints note documented (already platform-aware from Phase 13)
</success_criteria>

<output>
After completion, create `.planning/phases/15-cross-platform-docs-install/15-01-SUMMARY.md`
</output>
