---
phase: 06-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".planning/phases/06-audit-cleanup/06-FINDINGS.md"]
autonomous: true
requirements: [AUDIT-05]

must_haves:
  truths:
    - "A written findings report exists documenting all issues before any code changes are made"
    - "The report catalogs dead code, spinner duplication, and error handling inconsistencies with exact file locations"
  artifacts:
    - path: ".planning/phases/06-audit-cleanup/06-FINDINGS.md"
      provides: "Audit findings report with actionable issue inventory"
      contains: "cleanup_all"
  key_links: []
---

<objective>
Write a codebase audit findings report documenting all issues found during research before any code changes are made.

Purpose: AUDIT-05 requires a written findings report to exist before remediation begins. This creates the audit trail and ensures all issues are documented with exact locations, severity, and recommended fixes.

Output: `.planning/phases/06-audit-cleanup/06-FINDINGS.md`
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audit-cleanup/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run audit and produce findings report</name>
  <files>.planning/phases/06-audit-cleanup/06-FINDINGS.md</files>
  <action>
Run `cargo clippy -- -D warnings` to confirm current state. Then inspect the codebase to verify research findings are still accurate. Create `.planning/phases/06-audit-cleanup/06-FINDINGS.md` with:

1. **Audit Summary** -- date, scope (7 source files, ~1000 LOC), clippy status
2. **Dead Code** -- `TempFileRegistry::cleanup_all()` at `src/temp.rs:25-31` with `#[allow(dead_code)]` suppression. Recommendation: delete (Pipeline phase will share TempFileRegistry directly).
3. **Spinner Duplication** -- 5 identical spinner factory implementations:
   - `src/commands/cut.rs` line ~25 (`fn make_spinner`)
   - `src/commands/caption.rs` line ~264 (`fn make_spinner`)
   - `src/commands/overlay.rs` line ~42 (inlined in `generate_title()`)
   - `src/commands/normalize.rs` line ~63 (inlined in `normalize_to_temp()`)
   - `src/ffmpeg.rs` line ~184 (private `run_ffmpeg_with_spinner`)
   All use identical Braille tick strings and cyan style. Recommendation: extract to `src/ui.rs`.
4. **Error Handling Inconsistencies** -- 4 bare `anyhow::bail!` and 3+ bare `anyhow::anyhow!` usages bypassing the AppError system:
   - `commands/cut.rs:135` -- "No speech detected" (needs `NoSpeechDetected` variant)
   - `commands/overlay.rs:82` -- "claude CLI failed" (needs `ClaudeFailed` variant)
   - `commands/overlay.rs:90` -- "claude returned empty title" (same variant)
   - `commands/overlay.rs:194` -- "transcription file not found" (reuse `InputNotFound`)
   - `commands/caption.rs:443` -- JSON parse error (needs `ParseFailed` variant)
   - `commands/caption.rs:497` -- JSON serialize error (same variant)
   - `commands/overlay.rs:27` -- JSON parse error (same variant)
   - `commands/normalize.rs:98` -- loudnorm parse error (same variant)
   Recommendation: add `NoSpeechDetected`, `ClaudeFailed`, `ParseFailed` AppError variants.
5. **Clippy Status** -- passes with zero warnings; one suppression attribute to remove with dead code deletion.

Format as a structured markdown document with tables for each finding category. Include line numbers and recommended fix for each issue. This is NOT a code change -- only documentation.
  </action>
  <verify>File `.planning/phases/06-audit-cleanup/06-FINDINGS.md` exists and contains sections for dead code, spinner duplication, and error handling with specific file:line references.</verify>
  <done>A complete findings report exists documenting all audit issues with exact locations and recommendations, committed before any code changes are made.</done>
</task>

</tasks>

<verification>
- `.planning/phases/06-audit-cleanup/06-FINDINGS.md` exists
- Report contains dead code, spinner duplication, and error handling sections
- No source files in `src/` have been modified
- `cargo clippy -- -D warnings` still passes (baseline unchanged)
</verification>

<success_criteria>
Written findings report exists with all issues documented. No code changes made. Baseline clippy state confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audit-cleanup/06-01-SUMMARY.md`
</output>
