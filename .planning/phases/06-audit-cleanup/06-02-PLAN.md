---
phase: 06-audit-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: ["src/temp.rs", "src/ui.rs", "src/main.rs", "src/commands/cut.rs", "src/commands/caption.rs", "src/commands/overlay.rs", "src/commands/normalize.rs", "src/ffmpeg.rs"]
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "No #[allow(dead_code)] suppression attributes exist in the codebase"
    - "All spinner creation goes through a single shared utility function"
    - "cargo clippy -D warnings passes with zero warnings after dead code removal"
  artifacts:
    - path: "src/ui.rs"
      provides: "Shared spinner factory"
      exports: ["make_spinner"]
      min_lines: 10
    - path: "src/temp.rs"
      provides: "TempFileRegistry without dead cleanup_all method"
    - path: "src/main.rs"
      provides: "Crate root with mod ui declaration"
      contains: "mod ui"
  key_links:
    - from: "src/commands/cut.rs"
      to: "src/ui.rs"
      via: "crate::ui::make_spinner import"
      pattern: "crate::ui::make_spinner"
    - from: "src/commands/caption.rs"
      to: "src/ui.rs"
      via: "crate::ui::make_spinner import"
      pattern: "crate::ui::make_spinner"
    - from: "src/ffmpeg.rs"
      to: "src/ui.rs"
      via: "crate::ui::make_spinner import"
      pattern: "crate::ui::make_spinner"
---

<objective>
Remove dead code and extract duplicate spinner factories into a shared utility module.

Purpose: Eliminate the one dead code suppression attribute (AUDIT-01/02) and consolidate 5 duplicate spinner implementations into `src/ui.rs` (AUDIT-03).

Output: New `src/ui.rs` module, cleaned `src/temp.rs`, updated spinner call sites in all command files and `ffmpeg.rs`.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audit-cleanup/06-RESEARCH.md
@src/temp.rs
@src/main.rs
@src/commands/cut.rs
@src/commands/caption.rs
@src/commands/overlay.rs
@src/commands/normalize.rs
@src/ffmpeg.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code and create shared spinner module</name>
  <files>src/temp.rs, src/ui.rs, src/main.rs</files>
  <action>
1. In `src/temp.rs`, delete `cleanup_all()` AND its `#[allow(dead_code)]` attribute (both lines 25-31). The Ctrl-C handler in `register_cleanup()` handles cleanup directly via the Arc. Pipeline (Phase 8) will share TempFileRegistry and won't need this method.

2. Create `src/ui.rs` with a single public function:
```rust
use std::time::Duration;
use indicatif::{ProgressBar, ProgressStyle};

pub fn make_spinner(message: impl Into<String>) -> ProgressBar {
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::with_template("{spinner:.cyan} {msg}")
            .unwrap()
            .tick_strings(&[
                "\u{2800}", "\u{2801}", "\u{2809}", "\u{2819}", "\u{281b}", "\u{283b}",
                "\u{2839}", "\u{2838}", "\u{2830}", "\u{2820}", "\u{2800}", "\u{2713}",
            ]),
    );
    pb.enable_steady_tick(Duration::from_millis(80));
    pb.set_message(message.into());
    pb
}
```

3. In `src/main.rs`, add `mod ui;` alongside the other module declarations (after `mod temp;`).
  </action>
  <verify>`cargo check` passes. Grep for `#[allow(dead_code)]` in `src/` returns zero results. `src/ui.rs` exists with `pub fn make_spinner`.</verify>
  <done>`cleanup_all()` and its suppression attribute are deleted. `src/ui.rs` exists with the shared spinner factory. `mod ui` declared in `main.rs`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all spinner duplicates with shared utility</name>
  <files>src/commands/cut.rs, src/commands/caption.rs, src/commands/overlay.rs, src/commands/normalize.rs, src/ffmpeg.rs</files>
  <action>
Replace all 5 spinner factory implementations with calls to `crate::ui::make_spinner(...)`:

1. **`src/commands/cut.rs`**: Delete the local `fn make_spinner(message: String) -> ProgressBar` function (~line 25). Add `use crate::ui;` at the top. Replace all call sites: `make_spinner(msg)` becomes `ui::make_spinner(msg)`.

2. **`src/commands/caption.rs`**: Delete the local `fn make_spinner(message: String) -> ProgressBar` function (~line 264). Add `use crate::ui;` at the top. Replace all call sites similarly. Also remove the now-unused `use indicatif::{ProgressBar, ProgressStyle};` and `use std::time::Duration;` imports IF they are only used by the deleted spinner function (check carefully -- caption.rs may use ProgressBar for progress bars too, not just spinners).

3. **`src/commands/overlay.rs`**: The spinner is inlined in `generate_title()` (~line 42). Replace the inline `ProgressBar::new_spinner()` + style setup + `enable_steady_tick` block with `let spinner = crate::ui::make_spinner("Generating title with Claude...");`. Remove unused indicatif imports if no other progress bars exist in this file.

4. **`src/commands/normalize.rs`**: The spinner is inlined in `normalize_to_temp()` (~line 63). Replace the inline spinner creation block with `let spinner = crate::ui::make_spinner("Analyzing loudness...");` (or whatever the current message is). Remove unused indicatif/Duration imports if they are only used by the spinner.

5. **`src/ffmpeg.rs`**: The private `run_ffmpeg_with_spinner` function (~line 184) creates its own spinner. Replace its inline spinner creation with `crate::ui::make_spinner(msg)`. Keep the function -- it still wraps `run_ffmpeg` with spinner lifecycle. Remove unused indicatif imports if only used by the deleted spinner setup.

IMPORTANT: Do NOT remove `ProgressBar` imports from files that use `ProgressBar` for progress bars (not spinners). `caption.rs` and `cut.rs` likely have progress bar usage beyond spinners. Check each file's full usage before removing imports.
  </action>
  <verify>`cargo clippy -- -D warnings` passes with zero warnings. Grep for `fn make_spinner` in `src/commands/` returns zero results. Grep for `crate::ui::make_spinner\|ui::make_spinner` shows usage in all 5 consumer files.</verify>
  <done>All 5 duplicate spinner implementations replaced with calls to `crate::ui::make_spinner`. No local `make_spinner` functions remain in command files. Clippy passes clean with zero warnings and zero suppression attributes.</done>
</task>

</tasks>

<verification>
- `cargo clippy -- -D warnings` passes with zero warnings
- `grep -r "#\[allow(dead_code)\]" src/` returns empty
- `grep -r "fn make_spinner" src/commands/` returns empty
- `grep -r "crate::ui::make_spinner\|ui::make_spinner" src/` shows all 5 consumer files
- `src/ui.rs` exists with `pub fn make_spinner`
- `cargo test` passes (no regressions)
</verification>

<success_criteria>
Dead code removed, zero clippy warnings, zero suppression attributes. All spinner creation flows through `src/ui.rs`. No duplicate spinner factory functions exist.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audit-cleanup/06-02-SUMMARY.md`
</output>
